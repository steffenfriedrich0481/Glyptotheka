# Example environment configuration
# Copy this file to .env and adjust values

# Path to your 3D print projects directory
# This will be mounted read-only into the container
PROJECTS_PATH=/path/to/your/3d-prints

# Database path (inside container)
DATABASE_PATH=/app/data/glyptotheka.db

# Cache directory (inside container)  
CACHE_DIR=/app/cache

# Logging level
RUST_LOG=info,glyptotheka_backend=debug

# Ignored keywords for search display names and STL categorization (comma-separated)
# Folders matching these keywords (case-insensitive, substring match) will be:
# 1. Skipped when displaying project names in search results
# 2. Treated as STL category folders (not separate projects) during scanning
# Examples: "1 inch", "2 inch", "40 mm" folders will be categories under their parent project
IGNORED_KEYWORDS=PRESUPPORTED_STL,STL,UNSUPPORTED_STL,Unsupported,Pre-Supported,inch,mm

# Optional: Backend port (default 3000)
# BACKEND_PORT=3000

# Optional: Frontend port (default 8080)
# FRONTEND_PORT=8080
